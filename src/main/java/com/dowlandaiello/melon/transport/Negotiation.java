/**
 * Implements a set of upgradable, generic network transports.
 */
package com.dowlandaiello.melon.transport;

import java.net.Socket;

/**
 * Implements a simple negotiation protocol.
 * 
 * @author Dowland Aiello
 * @since 1.0
 */
public class Negotiation {
    /**
     * 
     * @param baseTransport the transport to negotiate from
     * @return
     */
    public static Socket negotiateSocket(Transport transport, String address) {

    }
}